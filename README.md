# portafolio-CV

URL Portafolio: https://portafolio-carlos-tafur.netlify.app/

URL de hoja de vida https://cv-carlos-tafur.netlify.app/ 

Instruciones: 
1. Configurar para packege.json -> "build": "set NODE_OPTIONS=--openssl-legacy-provider && ng build --configuration production", 
2. Ejecutar en consola -> export NODE_OPTIONS=--openssl-legacy-provider && ng build --configuration production //para ejecutar en local
3. ng serve
